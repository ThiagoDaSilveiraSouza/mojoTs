import styled from "styled-components";

export const UpperTitle = styled.h4`
  height: 22px;
  font-size: 18px;
  letter-spacing: 0;
  text-align: left;
  font-weight: 300;
  letter-spacing: 0px;
  color: var(--primary-color);
  margin: 0;
`;
